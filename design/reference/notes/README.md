# Screen Notes

Each note describes the reference intent, production files to inspect, states that need visual verification, screen-specific risks, and the recommended edit boundary for future pixel-alignment work.

These notes are not permission to change behavior. Container files, contracts, mappings, services, server code, Firebase/Auth/API code, and Firestore rules are inspection-only for pixel work unless a separate task explicitly changes behavior.
